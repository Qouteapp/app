import type { FC } from '../../../../lib/teact/teact';
import React from '../../../../lib/teact/teactn';

const FirstWorkspace: FC = () => {
  return <div>Onboarding FirstWorkspace</div>;
};

export default FirstWorkspace;
